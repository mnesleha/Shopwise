$kind: http-request
url: "{{base_url}}/api/v1/orders/{{order_id}}/"
method: GET
scripts:
  - type: afterResponse
    code: "pm.test(\"Order detail returns 403 for anonymous\", function () {\r

      \    pm.response.to.have.status(403);\r

      });\r

      \r

      \r\n"
    language: text/javascript
order: 1000
