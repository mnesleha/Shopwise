$kind: http-request
url: "{{base_url}}/api/v1/cart/"
method: GET
headers:
  Cookie: sessionid=mnnm798zmdl1kxxzzh9hpz87pawezhon
scripts:
  - type: afterResponse
    code: "pm.test(\"New cart is created after checkout\", function () {\r

      \    const data = pm.response.json();\r

      \    pm.expect(data.status).to.eql(\"ACTIVE\");\r

      \    pm.expect(data.items.length).to.eql(0);\r

      });\r\n"
    language: text/javascript
order: 7000
