$kind: http-request
url: "{{base_url}}/api/v1/categories/{{leaf_category_id}}/"
method: GET
scripts:
  - type: afterResponse
    code: "pm.test(\"Leaf category detail returns 404\", function () {\r

      \    pm.response.to.have.status(404);\r

      });\r\n"
    language: text/javascript
order: 3000
